# Integration Tests

End-to-end tests that run both contracts and the relayer against local testnets.

**Status:** Not yet implemented — see Layer 4 issues.

Prerequisites:
- Local Ethereum node (Hardhat network)
- Local Stellar node (Quickstart docker image)
- Both contracts deployed
- Relayer running
